# Build output and deps are already covered by the repo-root .gitignore
# (dist/, node_modules/). This file exists to re-include the env TEMPLATE, which
# the root's `.env*` rule would otherwise hide — the README tells users to copy it.
!.env.example
