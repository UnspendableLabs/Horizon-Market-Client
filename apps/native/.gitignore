# Native projects are generated by `expo prebuild` (Continuous Native Generation) —
# don't commit them; regenerate with `npx expo prebuild` / `npx expo run:android`.
/android
/ios

# Expo local cache / dev state
.expo/

# Dependencies & build output
node_modules/
*.log
