{
  "name": "horizon-market-cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Example CLI (`horizon`) for the Horizon Market Atomic Swap SDK — init / list / balances / sell / buy / send.",
  "bin": {
    "horizon": "./dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx watch src/index.ts",
    "horizon": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "@unspendablelabs/horizon-market-client": "file:../..",
    "citty": "^0.2.2",
    "cli-table3": "^0.6.5",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsup": "^8.5.1",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "esbuild": "0.27.7"
  }
}
