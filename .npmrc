# @kontor/sdk declares an *optional* peer on vite@^8, which conflicts with the
# vite@5 pulled by the dev dependency vitest. The vite peer is optional and unused
# by this library's code path, so allow npm to install past the peer conflict.
legacy-peer-deps=true
